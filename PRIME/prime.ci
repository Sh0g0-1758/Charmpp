mainmodule prime{
    mainchare Main{
        entry Main(CkArgMsg* m);
        entry void report(int idx,bool result);
    }
    chare Worker{
        entry Worker(int p,int idx,CProxy_Main main);
    }
}