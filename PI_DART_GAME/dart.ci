mainmodule dart {
    mainchare start {
        entry start(CkArgMsg*);
        entry void record(int circle);
    }

    chare worker {
        entry worker(int work, int rand_seed, CProxy_start s);
        entry void throw_dart();
    }
}
