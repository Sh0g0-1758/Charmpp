mainmodule primal {
    mainchare start {
        entry start(CkArgMsg* m);
        entry void result(int index, bool b);
    }

    chare test {
        entry test(int a, int index, CProxy_start parent);
    }
}
